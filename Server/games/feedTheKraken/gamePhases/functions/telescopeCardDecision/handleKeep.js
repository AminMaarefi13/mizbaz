/**
 * مدیریت تصمیم "keep" (نگه داشتن کارت تلسکوپ)
 * @param {Object} gameState - وضعیت فعلی بازی
 * @param {Object} card - کارت تلسکوپ
 */
function handleKeep(gameState, card) {
  // کارت روی دسته می‌ماند، هیچ کاری نیاز نیست
}

module.exports = handleKeep;
